#pragma once

#include "libraries.hpp"

stringstream getText(const string &text);

string getTextAsString(const string &text);

string myHash(const string &input);

string fileToHashCode(const string &text);
